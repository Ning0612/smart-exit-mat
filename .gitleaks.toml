title = "smart-exit-mat gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Historical documented CSRF nonce example; not an external credential"
regexTarget = "match"
regexes = ["a1b2c3d4e5f6a7b8"]
